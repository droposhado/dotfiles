name: Daily check packages

on:

  schedule:
    - cron:  '01 00 * * *'

  workflow_dispatch:

jobs:

  check-homebrew-packages:
    runs-on: ubuntu-latest
    steps:

      - name: checkout repo
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: 3.12.0

      - name: Install dependencies 
        run: pip install requests

      - name: Check packages
        run: python scripts/check_packages_homebrew.py

  # check-apt-packages:
  #   runs-on: ubuntu-latest
  #   steps:

  #     - name: checkout repo
  #       uses: actions/checkout@v6

  #     - name: Set up Python
  #       uses: actions/setup-python@v7
  #       with:
  #         python-version: 3.12.0

  #     - name: Check packages
  #       run: python scripts/check_packages_apt.py
